<!--
Configuration Default file as per version V 1.0.0.0

This file can be used to set the parameters of the DK modules at startup.
For the OpenDDS settings see the OpenDDS Configuration settings.
-->
<dds>
  <!-- This contains all the DK configurations settings -->
  <global>
    <!-- Contains global settings -->
    <!-- TODO IMPLEMENT -->
  </global>

  <modules>

    <ddsmonitor>
      <!--  QoS file contains the name of the XML file with the OpenDDS QOS settings.
        The settings apply to all the topics. -->

      <ddsqos>DDSMonitorQoS.xml</ddsqos>
      <subscriber>
        <!-- Contains User Subscriber settings -->
        <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
      </subscriber>
    </ddsmonitor>


    <monuser>
      <!--  QoS file contains the name of the XML file with the OpenDDS QOS settings.
        The settings apply to all the topics. -->

      <ddsqos>MonUserQoS.xml</ddsqos>
      <subscriber>
        <!-- Contains User Subscriber settings -->
        <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
      </subscriber>
    </monuser>

    <element>
      <!--  QoS file contains the name of the XML file with the OpenDDS QOS settings.
        The settings apply to all the topics. -->
      <ddsqos>ElementQoS.xml</ddsqos>
      <publisher>
        <!-- Contains Element Publisher settings -->
        <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
      </publisher>
      <subscriber>
        <!-- Contains Element Subscriber settings -->
        <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
      </subscriber>
    </element>

    <epdds>
      <!--  QoS file contains the name of the XML file with the OpenDDS QOS settings.
        The settings apply to all the topics. -->
      <ddsqos>EpDDSQoS.xml</ddsqos>

      <publisher>
        <!-- Contains EpDDS Request settings -->
        <infolevel>3</infolevel> <!-- Infolevel determines the level of output when running the module -->
      </publisher>

      <subscriber>
        <!-- Contains EpDDS Reply settings -->
        <infolevel>3</infolevel> <!-- Infolevel determines the level of output when running the module -->
        <!--
          <filter>
            <request> <modulename>todo</modulename>  </request>
            <reply> <modulename>todo</modulename>  </reply>
          </filter>
        -->
      </subscriber>
    </epdds>

    <cedds>
      <!--  QoS file contains the name of the XML file with the OpenDDS QOS settings.
        The settings apply to all the topics. -->
      <ddsqos>CEDDSQoS.xml</ddsqos>

      <publisher>
        <!-- Contains CE Request settings -->
        <infolevel>0</infolevel> <!-- Infolevel determines the level of output when running the module -->
      </publisher>

      <subscriber>
        <!-- Contains CE Reply settings -->
        <infolevel>0</infolevel> <!-- Infolevel determines the level of output when running the module -->
          <!-- <filter>
            <request> <modulename>A</modulename>  </request>
            <reply> <modulename>A</modulename>  </reply>
          </filter> -->
      </subscriber>
    </cedds>

    <event>
      <!--  QoS file contains the name of the XML file with the OpenDDS QOS settings.
        The settings apply to all the topics. -->
      <ddsqos>EventQoS.xml</ddsqos>
      <publisher>
        <!-- Contains Event Publisher settings -->
        <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
      </publisher>
      <subscriber>
        <!-- Contains Event Subscriber settings -->
        <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
      </subscriber>
    </event>

    <command>
      <!--  QoS file contains the name of the XML file with the OpenDDS QOS settings.
        The settings apply to all the topics. -->
    <!--      <ddsqos>CommandQoS.xml</ddsqos>-->

      <publisher>
        <!-- Contains Command Publisher settings -->
        <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
      </publisher>

      <subscriber>
        <!-- Contains Command Subscriber settings -->
        <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
      </subscriber>

    </command>

    <rpc>
      <!--  QoS file contains the name of the XML file with the OpenDDS QOS settings.
        The settings apply to all the topics. -->
      <ddsqos>RPCQoS.xml</ddsqos>

      <publisher>
        <!-- Contains RPC Request settings -->
        <infolevel>3</infolevel> <!-- Infolevel determines the level of output when running the module -->
      </publisher>

      <subscriber>
        <!-- Contains RPC Reply settings -->
        <infolevel>3</infolevel> <!-- Infolevel determines the level of output when running the module -->
        <!--
          <filter>
            <request> <modulename>todo</modulename>  </request>
            <reply> <modulename>todo</modulename>  </reply>
          </filter>
        -->
      </subscriber>
    </rpc>

    <user>
      <!--  QoS file contains the name of the XML file with the OpenDDS QOS settings.
        The settings apply to all the topics. -->
      <ddsqos>UserQoS.xml</ddsqos>
      <publisher>
        <!-- Contains User Publisher settings -->
        <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
      </publisher>
      <subscriber>
        <!-- Contains User Subscriber settings -->
        <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
      </subscriber>
    </user>

    <vehicle>
      <!--  QoS file contains the name of the XML file with the OpenDDS QOS settings.
        The settings apply to all the topics. -->
      <ddsqos>VehicleQoS.xml</ddsqos>

      <publisher>
        <!-- Contains Vehicle Publisher settings -->
        <data>
          <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
        </data>
      </publisher>
      <subscriber>
        <!-- Contains Vehicle Subscriber settings -->
        <data>
          <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
        </data>
      </subscriber>
    </vehicle>

    <control>
      <!--  QoS file contains the name of the XML file with the OpenDDS QOS settings.
        The settings apply to all the topics. -->
      <ddsqos>ControlQoS.xml</ddsqos>

      <publisher>
        <!-- Contains Control Publisher settings -->
        <data>
          <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
        </data>
      </publisher>
      <subscriber>
        <!-- Contains Control Subscriber settings -->
        <data>
          <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
        </data>
      </subscriber>
    </controle>

    <position>
      <!--  QoS file contains the name of the XML file with the OpenDDS QOS settings.
        The settings apply to all the topics. -->
      <ddsqos>PositionQoS.xml</ddsqos>

      <publisher>
        <!-- Contains Position Publisher settings -->
        <data>
          <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
        </data>
      </publisher>
      <subscriber>
        <!-- Contains Vehicle Subscriber settings -->
        <data>
          <infolevel>1</infolevel> <!-- Infolevel determines the level of output when running the module -->
        </data>
      </subscriber>
    </position>


    <media>
      <!--  QoS file contains the name of the XML file with the OpenDDS QOS settings.
        The settings apply to all the topics. -->
      <ddsqos>MediaQoS.xml</ddsqos>
      <publisher>
        <!-- Contains User Publisher settings -->
        <infolevel>10</infolevel> <!-- Infolevel determines the level of output when running the module -->
      </publisher>
      <subscriber>
        <!-- Contains User Subscriber settings -->
        <infolevel>10</infolevel> <!-- Infolevel determines the level of output when running the module -->
      </subscriber>
    </media>

  </modules>

</dds>
